#import <Cocoa/Cocoa.h>

int main(int argc, const char *argv[]) {
  @autoreleasepool {
    // Simple container app that does nothing but host the plugin
    return NSApplicationMain(argc, argv);
  }
}
